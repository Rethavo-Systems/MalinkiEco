{
  "rules": {
    ".read": false,
    ".write": false,
    "notification_signals": {
      "$signalId": {
        ".read": false,
        ".write": "!data.exists() && auth != null && newData.hasChildren(['jobId', 'creatorId', 'createdAtClient', 'dueAtClient']) && newData.child('creatorId').isString() && newData.child('creatorId').val() === auth.uid && newData.child('jobId').isString() && newData.child('jobId').val().length > 0 && newData.child('createdAtClient').isNumber() && newData.child('dueAtClient').isNumber()"
      }
    }
  }
}
